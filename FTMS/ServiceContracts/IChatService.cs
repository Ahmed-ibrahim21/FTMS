using FTMS.DTOs;

namespace FTMS.ServiceContracts
{
    public interface IChatService
    {
        //Task<IEnumerable<GetGroupChatDto>> GetGroupChatsAsync(string userId);
        //Task<ChatDto> GetChatAsync(Guid id);
        //Task<ChatDto> CreateChatAsync(ChatDto chatDto);
    }
}
